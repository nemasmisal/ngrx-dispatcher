import { NgrxDispatcherDirective } from './ngrx-dispatcher.directive';

describe('NgrxDispatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new NgrxDispatcherDirective();
    expect(directive).toBeTruthy();
  });
});
